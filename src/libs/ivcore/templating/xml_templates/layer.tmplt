{% comment %}
Template for generation of XML scheme of layer.
The following tags are available in this sub-template:
layer            : Instance of layer (provided by a top level template)
layer.name       :   |_ Layer name
layer.is_visible :   |_ Layer visibility
{% endcomment %}
<Layer name="{{ layer.name }}" is_visible="{{ layer.is_visible }}"/>
