/* CIF PROCESS (250, 150), (150, 75) */
process Ponger;
    /* CIF TEXT (907, 193), (267, 140) */
    -- Text area for declarations and comments
    
    dcl pingArg My_Sequence;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
        /* CIF input (429, 65), (104, 35) */
        input Ping(pingArg);
            /* CIF output (447, 120), (70, 35) */
            output Pong;
            /* CIF NEXTSTATE (447, 175), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Ponger;