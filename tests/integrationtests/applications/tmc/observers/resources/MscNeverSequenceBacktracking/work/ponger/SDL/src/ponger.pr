/* CIF PROCESS (250, 150), (150, 75) */
process Ponger;
    /* CIF TEXT (865, 120), (267, 140) */
    -- Text area for declarations and comments
    
    dcl counter MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (352, 14), (70, 35) */
    START;
        /* CIF task (340, 69), (92, 35) */
        task counter := 0;
        /* CIF NEXTSTATE (352, 119), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (577, 61), (70, 35) */
    state Wait;
        /* CIF input (577, 116), (70, 35) */
        input Ping;
            /* CIF task (536, 171), (151, 35) */
            task counter := counter + 1;
            /* CIF decision (577, 226), (70, 50) */
            decision counter;
                /* CIF ANSWER (532, 296), (70, 23) */
                (3):
                    /* CIF output (532, 339), (70, 35) */
                    output Pong;
                    /* CIF task (519, 394), (95, 35) */
                    task counter := 0;
                /* CIF ANSWER (622, 296), (70, 23) */
                else:
            enddecision;
            /* CIF NEXTSTATE (577, 445), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Ponger;