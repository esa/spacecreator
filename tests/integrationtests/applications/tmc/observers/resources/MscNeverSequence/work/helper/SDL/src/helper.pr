/* CIF PROCESS (250, 150), (150, 75) */
process Helper;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
        /* CIF input (450, 65), (70, 35) */
        input Tick;
            /* CIF output (450, 120), (70, 35) */
            output HelpMe;
            /* CIF NEXTSTATE (450, 175), (70, 38) */
            NEXTSTATE Wait;
    endstate;
endprocess Helper;