/* CIF PROCESS (259, 156), (150, 75) */
process Helper;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (456, 15), (70, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    state Wait;
        /* CIF input (456, 70), (70, 35) */
        input Tick;
            /* CIF output (456, 125), (70, 35) */
            output HelpMe;
            /* CIF NEXTSTATE (456, 180), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Helper;