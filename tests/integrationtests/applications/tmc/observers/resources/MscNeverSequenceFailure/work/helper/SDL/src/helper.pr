/* CIF PROCESS (250, 150), (150, 75) */
process Helper;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (457, 20), (70, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    state Wait;
        /* CIF input (457, 75), (70, 35) */
        input Tick;
            /* CIF output (457, 130), (70, 35) */
            output HelpMe;
            /* CIF NEXTSTATE (457, 185), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Helper;