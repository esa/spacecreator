/* CIF PROCESS (250, 150), (150, 75) */
process Ponger;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (454, 15), (70, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    state Wait;
        /* CIF input (454, 70), (70, 35) */
        input Ping;
            /* CIF NEXTSTATE (454, 125), (70, 35) */
            NEXTSTATE Ready;
    endstate;
    /* CIF state (601, 18), (70, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    state Ready;
        /* CIF input (601, 73), (70, 35) */
        input HelpMe;
            /* CIF output (601, 128), (70, 35) */
            output Pong;
            /* CIF NEXTSTATE (601, 183), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Ponger;