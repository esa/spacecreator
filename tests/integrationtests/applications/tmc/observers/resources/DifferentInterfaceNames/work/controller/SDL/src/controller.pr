/* CIF PROCESS (237, 142), (150, 75) */
process Controller;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF output (320, 65), (70, 35) */
        output left;
        /* CIF NEXTSTATE (320, 115), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (589, 81), (70, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    state Wait;
    endstate;
endprocess Controller;