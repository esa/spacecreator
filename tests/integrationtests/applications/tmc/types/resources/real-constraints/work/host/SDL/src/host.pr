/* CIF PROCESS (251, 150), (150, 75) */
process Host;
    /* CIF TEXT (851, 187), (267, 140) */
    -- Text area for declarations and comments
    dcl param MyReal;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (525, 28), (70, 35) */
    state Wait;
        /* CIF input (511, 89), (94, 35) */
        input Ping(param);
            /* CIF output (509, 152), (98, 35) */
            output Pong(param);
            /* CIF NEXTSTATE (524, 207), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Host;