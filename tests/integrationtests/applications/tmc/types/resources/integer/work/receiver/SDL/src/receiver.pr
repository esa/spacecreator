/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1334, 60), (267, 143) */
    -- Text area for declarations and comments
    
    dcl integerArg MyBetterInteger;
    dcl integerValue MyBetterInteger := 2;
    dcl integerHandled T_Boolean := false;
    /* CIF ENDTEXT */
    /* CIF START (437, 12), (70, 35) */
    START;
        /* CIF NEXTSTATE (437, 67), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (644, 14), (70, 35) */
    state Wait;
        /* CIF input (567, 73), (225, 44) */
        input IntegerInterface(integerArg);
            /* CIF task (589, 137), (179, 35) */
            task integerValue := integerArg;
            /* CIF task (600, 192), (158, 35) */
            task integerHandled := true;
            /* CIF NEXTSTATE (644, 242), (70, 38) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;