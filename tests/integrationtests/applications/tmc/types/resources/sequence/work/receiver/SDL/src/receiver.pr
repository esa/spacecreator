/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1334, 60), (324, 143) */
    -- Text area for declarations and comments
    
    dcl seqArg MyBetterSequence;
    dcl seqValue MyBetterSequence := { val1 0, val2 2 };
    dcl  seqHandled T_Boolean := false;
    /* CIF ENDTEXT */
    /* CIF START (402, 12), (70, 35) */
    START;
        /* CIF NEXTSTATE (402, 67), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (866, 36), (70, 35) */
    state Wait;
        /* CIF input (793, 91), (216, 40) */
        input SequenceInterface(seqArg);
            /* CIF task (833, 151), (135, 35) */
            task seqValue := seqArg;
            /* CIF task (832, 206), (136, 35) */
            task seqHandled := true;
            /* CIF NEXTSTATE (866, 261), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;