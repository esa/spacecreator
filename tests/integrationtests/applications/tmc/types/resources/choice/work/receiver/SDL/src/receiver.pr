/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1334, 60), (296, 143) */
    -- Text area for declarations and comments
    
    dcl choiceArg MyBetterChoice;
    dcl choiceValue MyBetterChoice := option1 : 2;
    dcl choiceHandled T_Boolean := false;
    /* CIF ENDTEXT */
    /* CIF START (437, 12), (70, 35) */
    START;
        /* CIF NEXTSTATE (437, 67), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (600, 20), (70, 35) */
    state Wait;
        /* CIF input (528, 75), (213, 43) */
        input ChoiceInterface(choiceArg) ;
            /* CIF task (550, 138), (169, 35) */
            task choiceValue := choiceArg;
            /* CIF task (558, 193), (153, 35) */
            task choiceHandled := true;
            /* CIF NEXTSTATE (600, 248), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;