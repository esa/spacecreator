/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1334, 60), (296, 143) */
    -- Text area for declarations and comments
    
    dcl choiceArg MyBetterChoice;
    dcl choiceValue MyBetterChoice;
    dcl choiceHandled T_Boolean := false;
    /* CIF ENDTEXT */
    /* CIF START (543, 34), (70, 35) */
    START;
        /* CIF task (491, 89), (173, 35) */
        task choiceValue := option1 : 2;
        /* CIF NEXTSTATE (543, 139), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (849, 25), (70, 35) */
    state Wait;
        /* CIF input (777, 80), (213, 43) */
        input ChoiceInterface(choiceArg);
            /* CIF task (799, 143), (169, 35) */
            task choiceValue := choiceArg;
            /* CIF task (807, 198), (153, 35) */
            task choiceHandled := true;
            /* CIF NEXTSTATE (849, 253), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;