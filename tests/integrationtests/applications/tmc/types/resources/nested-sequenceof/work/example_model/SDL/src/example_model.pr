/* CIF PROCESS (250, 150), (150, 75) */

process Example_Model;
    /* CIF Keep Specific Geode Partition 'default' */
    /* CIF TEXT (527, 267), (187, 140) */
    dcl param HighSequenceOf;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (1231, 191), (70, 35) */
    /* CIF Keep Specific Geode Partition 'default' */
    state Wait;
        /* CIF input (1196, 246), (139, 35) */
        input check_value(param);
            /* CIF NEXTSTATE (1231, 301), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Example_Model;