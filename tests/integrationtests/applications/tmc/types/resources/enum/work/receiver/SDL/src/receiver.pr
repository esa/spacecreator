/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1334, 60), (267, 143) */
    -- Text area for declarations and comments
    
    dcl enumArg MyBetterEnum;
    dcl enumValue MyBetterEnum := red;
    dcl enumHandled T_Boolean := false;
    /* CIF ENDTEXT */
    /* CIF START (437, 12), (70, 35) */
    START;
        /* CIF NEXTSTATE (437, 67), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (788, 86), (70, 35) */
    state Wait;
        /* CIF input (712, 141), (222, 53) */
        input EnumInterface(enumArg);
            /* CIF task (742, 214), (160, 35) */
            task enumValue := enumArg;
            /* CIF task (748, 269), (148, 35) */
            task enumHandled := true;
            /* CIF NEXTSTATE (788, 324), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;