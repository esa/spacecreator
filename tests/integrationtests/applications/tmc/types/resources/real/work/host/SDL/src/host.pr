/* CIF PROCESS (251, 150), (150, 75) */
process Host;
    /* CIF TEXT (637, 233), (267, 140) */
    -- Text area for declarations and comments
    dcl param MyReal;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (510, 30), (70, 35) */
    state Wait;
        /* CIF input (496, 85), (94, 35) */
        input Ping(param);
            /* CIF task (471, 140), (144, 35) */
            task param := param * 2.0;
            /* CIF output (494, 195), (98, 35) */
            output Pong(param);
            /* CIF NEXTSTATE (509, 250), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Host;