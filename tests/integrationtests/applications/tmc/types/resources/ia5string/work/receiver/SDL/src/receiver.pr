/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1038, 53), (267, 140) */
    dcl stringArg MyBetterString;
    dcl stringVal MyBetterString := "a";
    dcl stringHandled T_Boolean := false;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (539, 45), (70, 35) */
    state Wait;
        /* CIF input (471, 100), (206, 40) */
        input StringInterface(stringArg);
            /* CIF task (500, 160), (147, 35) */
            task stringVal := stringArg;
            /* CIF task (499, 215), (149, 35) */
            task stringHandled := true;
            /* CIF NEXTSTATE (539, 270), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;