/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1334, 60), (267, 143) */
    -- Text area for declarations and comments
    
    dcl boolArg MyBetterBoolean;
    dcl boolValue MyBetterBoolean := true;
    dcl boolHandled T_Boolean := false;
    /* CIF ENDTEXT */
    /* CIF START (437, 12), (70, 35) */
    START;
        /* CIF NEXTSTATE (437, 67), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (611, 7), (70, 38) */
    state Wait;
        /* CIF input (544, 65), (204, 50) */
        input BoolInterface(boolArg);
            /* CIF task (572, 135), (146, 31) */
            task boolValue := boolArg;
            /* CIF task (573, 186), (146, 35) */
            task boolHandled := true;
            /* CIF NEXTSTATE (611, 241), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;