/* CIF PROCESS (250, 150), (150, 75) */
process Ponger;
    /* CIF TEXT (688, 32), (267, 140) */
    -- Text area for declarations and comments
    
    dcl result MyInteger;
    dcl arg MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
        /* CIF input (417, 65), (135, 35) */
        input PingInterface(arg);
            /* CIF task (420, 120), (128, 35) */
            task result := arg + arg;
            /* CIF output (409, 175), (149, 35) */
            output PongInterface(result);
    endstate;
endprocess Ponger;