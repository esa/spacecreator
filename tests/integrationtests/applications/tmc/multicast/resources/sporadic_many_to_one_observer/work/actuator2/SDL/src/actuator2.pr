/* CIF PROCESS (250, 150), (150, 75) */
process Actuator2;
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (616, 110), (70, 35) */
    state Wait;
        /* CIF input (615, 165), (71, 35) */
        input trigger2;
            /* CIF output (585, 220), (130, 35) */
            output test_parameter(2);
            /* CIF NEXTSTATE (616, 275), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Actuator2;