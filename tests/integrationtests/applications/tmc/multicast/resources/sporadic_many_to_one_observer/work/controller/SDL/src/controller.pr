/* CIF PROCESS (250, 150), (150, 75) */
process Controller;
    /* CIF TEXT (380, 49), (267, 140) */
    -- Text area for declarations and comments
    dcl param MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (526, 208), (70, 35) */
    START;
        /* CIF output (525, 263), (71, 35) */
        output trigger1;
        /* CIF NEXTSTATE (526, 313), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (833, 231), (70, 35) */
    state Wait;
        /* CIF input (787, 286), (160, 35) */
        input test_parameter(param);
            /* CIF output (832, 341), (71, 35) */
            output trigger2;
            /* CIF NEXTSTATE (833, 396), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Controller;