/* Body file for function ErroredChannel
 * Generated by TASTE on 2022-09-30 02:17:32
 */
#include "erroredchannel_vm_if.h"
#include "C_ASN1_Types.h"
#include <stdio.h>
extern long long getTimeInMilliseconds(void);



extern void adainit(void);

unsigned erroredchannel_initialized = 0;
void init_erroredchannel(void)
{
   if (0 == erroredchannel_initialized) {
      // Initialize epoch for the MSC tracing in debug mode
      long long unused = getTimeInMilliseconds();
      erroredchannel_initialized = 1;
      // Initialize Ada runtime to call elaboration functions
      adainit();
      puts ("[TASTE] Initialization completed for function ErroredChannel");
      erroredchannel_initialized = 2;
   }
}
void erroredchannel_CorruptChar
      (const char *IN_p1, size_t IN_p1_len)

{
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: erroredchannel,corruptchar,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Declare user code function as external (it may not have a .h if it is in Ada)
   extern void erroredchannel_PI_CorruptChar
      (const asn1SccUINT8 *);





   // Call user code
   erroredchannel_PI_CorruptChar ((asn1SccUINT8 *)IN_p1);

   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *erroredchannel_state(void);
      printf ("INNER_SDL_STATE: erroredchannel,%s\n", erroredchannel_state());
      fflush(stdout);
   }
}




void erroredchannel_DropChar()
{
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: erroredchannel,dropchar,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Directly call user code, no parameters to pass
   extern void erroredchannel_PI_DropChar(void);
   erroredchannel_PI_DropChar();
   // puts ("erroredchannel_dropchar called");
   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *erroredchannel_state(void);
      printf ("INNER_SDL_STATE: erroredchannel,%s\n", erroredchannel_state());
      fflush(stdout);
   }
}




void erroredchannel_PHYTxChar
      (const char *IN_p1, size_t IN_p1_len)

{
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: erroredchannel,phytxchar,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Declare user code function as external (it may not have a .h if it is in Ada)
   extern void erroredchannel_PI_PHYTxChar
      (const asn1SccUINT8 *);





   // Call user code
   erroredchannel_PI_PHYTxChar ((asn1SccUINT8 *)IN_p1);

   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *erroredchannel_state(void);
      printf ("INNER_SDL_STATE: erroredchannel,%s\n", erroredchannel_state());
      fflush(stdout);
   }
}

