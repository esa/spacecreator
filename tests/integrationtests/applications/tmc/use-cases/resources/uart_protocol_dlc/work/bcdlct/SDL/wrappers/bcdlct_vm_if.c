/* Body file for function BCDLCT
 * Generated by TASTE on 2022-09-30 02:17:32
 */
#include "bcdlct_vm_if.h"
#include "C_ASN1_Types.h"
#include <stdio.h>
extern long long getTimeInMilliseconds(void);



extern void adainit(void);

unsigned bcdlct_initialized = 0;
void init_bcdlct(void)
{
   if (0 == bcdlct_initialized) {
      // Initialize epoch for the MSC tracing in debug mode
      long long unused = getTimeInMilliseconds();
      bcdlct_initialized = 1;
      // Initialize Ada runtime to call elaboration functions
      adainit();
      puts ("[TASTE] Initialization completed for function BCDLCT");
      bcdlct_initialized = 2;
   }
}
void bcdlct_DLCTxReset()
{
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: bcdlct,dlctxreset,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Directly call user code, no parameters to pass
   extern void bcdlct_PI_DLCTxReset(void);
   bcdlct_PI_DLCTxReset();
   // puts ("bcdlct_dlctxreset called");
   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *bcdlct_state(void);
      printf ("INNER_SDL_STATE: bcdlct,%s\n", bcdlct_state());
      fflush(stdout);
   }
}




void bcdlct_DLCTxSDU
      (const char *IN_p1, size_t IN_p1_len)

{
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: bcdlct,dlctxsdu,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Declare user code function as external (it may not have a .h if it is in Ada)
   extern void bcdlct_PI_DLCTxSDU
      (const asn1SccT_UART_DLC_SDU *);





   // Call user code
   bcdlct_PI_DLCTxSDU ((asn1SccT_UART_DLC_SDU *)IN_p1);

   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *bcdlct_state(void);
      printf ("INNER_SDL_STATE: bcdlct,%s\n", bcdlct_state());
      fflush(stdout);
   }
}




void bcdlct_PHYTxError()
{
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: bcdlct,phytxerror,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Directly call user code, no parameters to pass
   extern void bcdlct_PI_PHYTxError(void);
   bcdlct_PI_PHYTxError();
   // puts ("bcdlct_phytxerror called");
   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *bcdlct_state(void);
      printf ("INNER_SDL_STATE: bcdlct,%s\n", bcdlct_state());
      fflush(stdout);
   }
}

