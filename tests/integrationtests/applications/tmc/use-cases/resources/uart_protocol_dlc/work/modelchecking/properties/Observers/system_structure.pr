/* CIF Keep Specific Geode ASNFilename 'dataview-uniq.asn' */
use Datamodel;

system ProtocolObserver;
    /* CIF TEXT (783, 105), (388, 140) */
    /* CIF ENDTEXT */
    channel c
        from env to ProtocolObserver with DLCTxSDU;
        from ProtocolObserver to env with DLCTxSDU;
        from BCDLCT to ProtocolObserver with PHYTxChar;
        from ProtocolObserver to RTDLCR with PHYRxChar;
    endchannel;
    block ProtocolObserver;
        signalroute r
        from env to ProtocolObserver with DLCTxSDU;
        from ProtocolObserver to env with DLCTxSDU;
        connect c and r;
        /* CIF PROCESS (557, 255), (150, 75) */
        process ProtocolObserver referenced;
    endblock;
endsystem;


