/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (837, 97), (267, 140) */
    -- Text area for declarations and comments
    
    dcl pingArg MyBetterInteger;
    dcl pingTmp MyBetterInteger;
    dcl pingButArrayArg MyArray;
    dcl pingButArrayTmp MyArray;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (662, 11), (70, 35) */
    state Ok;
        /* CIF input (592, 66), (209, 35) */
        input PingButArray(pingButArrayArg);
            /* CIF task (575, 121), (243, 35) */
            task pingButArrayTmp := pingButArrayArg;
            /* CIF NEXTSTATE (662, 176), (70, 35) */
            NEXTSTATE Wait;
    endstate;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
        /* CIF input (432, 65), (104, 35) */
        input Ping(pingArg);
            /* CIF task (415, 120), (138, 35) */
            task pingTmp := pingArg;
            /* CIF NEXTSTATE (450, 175), (70, 35) */
            NEXTSTATE Ok;
    endstate;
endprocess Receiver;