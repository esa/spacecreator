/* CIF PROCESS (250, 150), (150, 75) */
process Receiver;
    /* CIF TEXT (1334, 60), (267, 143) */
    -- Text area for declarations and comments
    
    dcl pingArg MyBetterInteger;
    dcl temp MyArray;
    /* CIF ENDTEXT */
    /* CIF START (437, 12), (70, 35) */
    START;
        /* CIF NEXTSTATE (437, 67), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (644, 14), (70, 35) */
    state Wait;
        /* CIF input (613, 69), (132, 41) */
        input Ping(pingArg);
            /* CIF PROCEDURECALL (587, 130), (184, 35) */
            call writeln("Ping ", pingArg);
            /* CIF task (612, 185), (132, 35) */
            task temp := { pingArg };
            /* CIF output (616, 235), (125, 35) */
            output Pong(pingArg);
            /* CIF output (591, 290), (176, 35) */
            output PongButArray(temp);
            /* CIF NEXTSTATE (644, 340), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Receiver;