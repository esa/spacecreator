/* CIF PROCESS (250, 150), (150, 75) */
process Ponger;
    /* CIF TEXT (690, 77), (267, 140) */
    -- Text area for declarations and comments
    
    dcl pingArg MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
        /* CIF input (432, 65), (104, 35) */
        input Ping(pingArg);
            /* CIF PROCEDURECALL (423, 120), (122, 35) */
            call writeln(pingArg);
            /* CIF output (430, 175), (108, 35) */
            output Pong(pingArg);
            /* CIF NEXTSTATE (450, 230), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Ponger;