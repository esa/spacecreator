/* CIF PROCESS (250, 150), (150, 75) */
process Function_2;
    /* CIF TEXT (633, 174), (267, 140) */
    -- Text area for declarations and comments
    
    dcl myVar MySeq;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
        /* CIF input (434, 65), (93, 35) */
        input PI_1(myVar);
            /* CIF decision (403, 120), (156, 50) */
            decision exist(myVar.inputdata);
                /* CIF ANSWER (495, 190), (70, 23) */
                (false):
                    /* CIF NEXTSTATE (495, 233), (70, 35) */
                    NEXTSTATE Wait;
                /* CIF ANSWER (405, 190), (70, 23) */
                (true):
                    /* CIF NEXTSTATE (402, 233), (76, 35) */
                    NEXTSTATE Incorrect;
            enddecision;
    endstate;
    /* CIF state (581, 10), (76, 35) */
    state Incorrect;
    endstate;
endprocess Function_2;