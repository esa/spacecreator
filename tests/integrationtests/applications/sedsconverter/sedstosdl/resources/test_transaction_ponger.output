/* CIF Process (0, 0), (800, 100) */
process Ponger;
    /* CIF Text (0, 150), (400, 500) */
    dcl Pongvar Number;
    dcl Ping_PingCommand_Pi_transactionName CString;
    /* CIF ENDTEXT */
    /* CIF Procedure (0, 900), (800, 50) */
    procedure DoPongSmall;
        /* CIF Text (0, 0), (400, 500) */
        fpar
            in Pongvar Number;
        /* CIF ENDTEXT */
        /* CIF Start (0, 750), (200, 75) */
        START;
            /* CIF ProcedureCall (0, 937), (100, 50) */
            call Pong_PongCommand_Ri(Pongvar);
    return;
    endprocedure;
    /* CIF Procedure (0, 975), (800, 50) */
    procedure DoPongBig;
        /* CIF Text (0, 0), (400, 500) */
        fpar
            in Pongvar Number;
        /* CIF ENDTEXT */
        /* CIF Start (0, 750), (200, 75) */
        START;
            /* CIF ProcedureCall (0, 937), (100, 50) */
            call Pong_PongCommand_Ri(999);
    return;
    endprocedure;
    /* CIF Procedure (0, 1050), (800, 50) */
    procedure Ping_PingCommand_Pi;
        /* CIF Text (0, 0), (400, 500) */
        fpar
            in Pingarg Number,
            in sedsTransactionName CString;
        /* CIF ENDTEXT */
        /* CIF Start (0, 750), (200, 75) */
        START;
            /* CIF Task (0, 937), (100, 50) */
            task Ping_PingCommand_Pi_transactionName := sedsTransactionName;
            /* CIF Decision (0, 937), (400, 50) */
            decision sedsTransactionName;
                /* CIF Answer (0, 1012), (400, 50) */
                ("MakeItSmall"):
                    /* CIF ProcedureCall (0, 1087), (100, 50) */
                    call DoPongSmall(Pingarg);
                /* CIF Answer (600, 1012), (400, 50) */
                ("MakeItBig"):
                    /* CIF ProcedureCall (600, 1087), (100, 50) */
                    call DoPongBig(Pingarg);
            enddecision;
    return;
    endprocedure;
    /* CIF Start (0, 1125), (200, 75) */
    START;
        /* CIF NextState (0, 1312), (200, 50) */
        NEXTSTATE s1;
    /* CIF State (0, 1387), (200, 50) */
    state s1;
        /* CIF Input (0, 1387), (200, 50) */
        input Ping_PingCommand_Pi;
            /* CIF Decision (0, 1387), (400, 50) */
            decision Ping_PingCommand_Pi_transactionName;
                /* CIF Answer (0, 1462), (400, 50) */
                ("MakeItSmall"):
                    /* CIF NextState (0, 1537), (200, 50) */
                    NEXTSTATE s1;
                /* CIF Answer (1800, 1462), (400, 50) */
                ELSE:
            enddecision;
            /* CIF Decision (2400, 1462), (400, 50) */
            decision Ping_PingCommand_Pi_transactionName;
                /* CIF Answer (2400, 1537), (400, 50) */
                ("MakeItBig"):
                    /* CIF NextState (2400, 1612), (200, 50) */
                    NEXTSTATE s2;
                /* CIF Answer (3000, 1537), (400, 50) */
                ELSE:
            enddecision;
    endstate;
    /* CIF State (3900, 1387), (200, 50) */
    state s2;
    endstate;
endprocess Ponger;
