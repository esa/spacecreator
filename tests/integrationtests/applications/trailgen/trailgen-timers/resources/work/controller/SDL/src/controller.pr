/* CIF PROCESS (250, 150), (150, 75) */
process Controller;
    /* CIF TEXT (386, 363), (267, 140) */
    -- Text area for declarations and comments
    
    dcl test_param MyTestInteger;
    dcl param MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (903, 252), (70, 35) */
    state Wait;
        /* CIF input (876, 307), (121, 35) */
        input test(test_param);
            /* CIF output (873, 362), (127, 35) */
            output work(test_param);
            /* CIF NEXTSTATE (901, 417), (72, 35) */
            NEXTSTATE Working;
    endstate;
    /* CIF state (1223, 247), (72, 35) */
    state Working;
        /* CIF input (1198, 302), (122, 35) */
        input response(param);
            /* CIF output (1206, 357), (106, 35) */
            output report(param);
            /* CIF NEXTSTATE (1225, 412), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Controller;