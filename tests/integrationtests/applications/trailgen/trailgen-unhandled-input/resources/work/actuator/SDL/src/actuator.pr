/* CIF PROCESS (250, 150), (150, 75) */
process Actuator;
    /* CIF TEXT (401, 337), (267, 140) */
    -- Text area for declarations and comments
    
    dcl wrk MyInteger;
    /* CIF ENDTEXT */
    /* CIF START (363, 40), (70, 35) */
    START;
        /* CIF NEXTSTATE (363, 90), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (933, 57), (70, 35) */
    state Busy;
        /* CIF input (928, 112), (79, 35) */
        input ping(wrk);
            /* CIF output (933, 167), (70, 35) */
            output pong(3);
            /* CIF NEXTSTATE (929, 222), (77, 35) */
            NEXTSTATE Detected;
    endstate;
    /* CIF state (645, 29), (70, 35) */
    state Wait;
        /* CIF input (640, 84), (79, 35) */
        input ping(wrk);
            /* CIF output (645, 139), (70, 35) */
            output pong(0);
            /* CIF NEXTSTATE (645, 194), (70, 35) */
            NEXTSTATE Busy;
    endstate;
    /* CIF state (1242, 87), (77, 35) */
    state Detected;
    endstate;
endprocess Actuator;