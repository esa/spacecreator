/* File generated by kazoo for TASTE - do not edit */
#include "demo_polyorb_interface.h"

// Required interfaces of function Controller

// Required interface Calculate in function Controller
#include "qgenhost_vm_if.h"  // Remote language: QGenC
void vm_controller_calculate
        (const char *IN_buf_intparam, size_t size_IN_buf_intparam,
         const char *IN_buf_realparam, size_t size_IN_buf_realparam,
         const char *IN_buf_boolparam, size_t size_IN_buf_boolparam,
         char *OUT_buf_outparam, size_t *size_OUT_buf_outparam)

{
   // puts("[TASTE] Protected call of Calculate");
   pro_QGenHost_Calculate
        (IN_buf_intparam, size_IN_buf_intparam,
         IN_buf_realparam, size_IN_buf_realparam,
         IN_buf_boolparam, size_IN_buf_boolparam,
         OUT_buf_outparam, size_OUT_buf_outparam);

}

#include "qgenhost_vm_if.h"


void pro_QGenHost_Calculate
      (const char *IN_buf_intparam, size_t size_IN_buf_intparam,
       const char *IN_buf_realparam, size_t size_IN_buf_realparam,
       const char *IN_buf_boolparam, size_t size_IN_buf_boolparam,
       char *OUT_buf_outparam, size_t *size_OUT_buf_outparam)

{
   extern process_package__taste_protected_object qgenhost_protected;
   __po_hi_protected_lock (qgenhost_protected.protected_id);
   qgenhost_Calculate
      (IN_buf_intparam, size_IN_buf_intparam,
       IN_buf_realparam, size_IN_buf_realparam,
       IN_buf_boolparam, size_IN_buf_boolparam,
       OUT_buf_outparam, size_OUT_buf_outparam);

   __po_hi_protected_unlock (qgenhost_protected.protected_id);
}

#include "controller_vm_if.h"

// Function called by the middleware when task is ready to run
void call_controller_trigger (__po_hi_task_id sender_pid)
{
   (void)sender_pid; // sender id is not used for now

   controller_Trigger();
}
