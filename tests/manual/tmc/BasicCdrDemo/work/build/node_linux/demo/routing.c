/* File generated by kazoo for TASTE - do not edit */

#include "routing.h"

#include <stdio.h>
#include <stddef.h>
#include <stdint.h>



// Routing table of function Actuator

bool actuator_feedback_routing_table[PID_env];


void actuator_feedback_set_route_enabled(asn1SccPID destination, bool is_enabled)
{
    actuator_feedback_routing_table[destination] = is_enabled;
}

void actuator_feedback_set_recipient(asn1SccPID destination)
{
    actuator_feedback_set_all_routes_enabled(false);
    actuator_feedback_set_route_enabled(destination, true);
}

void actuator_feedback_set_all_routes_enabled(bool are_enabled)
{
    for (size_t i = 0; i < (size_t)PID_env; i++)
    {
        actuator_feedback_routing_table[i] = are_enabled;
    }
}



__thread asn1SccPID actuator_ctrl_sender_pid;

asn1SccPID actuator_ctrl_get_sender()
{
    return actuator_ctrl_sender_pid;
}




// Routing table of function EGSE

bool egse_tc_routing_table[PID_env];


void egse_tc_set_route_enabled(asn1SccPID destination, bool is_enabled)
{
    egse_tc_routing_table[destination] = is_enabled;
}

void egse_tc_set_recipient(asn1SccPID destination)
{
    egse_tc_set_all_routes_enabled(false);
    egse_tc_set_route_enabled(destination, true);
}

void egse_tc_set_all_routes_enabled(bool are_enabled)
{
    for (size_t i = 0; i < (size_t)PID_env; i++)
    {
        egse_tc_routing_table[i] = are_enabled;
    }
}



__thread asn1SccPID egse_poll_sender_pid;

asn1SccPID egse_poll_get_sender()
{
    return egse_poll_sender_pid;
}
__thread asn1SccPID egse_tm_sender_pid;

asn1SccPID egse_tm_get_sender()
{
    return egse_tm_sender_pid;
}




// Routing table of function ModeManager

bool modemanager_ctrl_routing_table[PID_env];


void modemanager_ctrl_set_route_enabled(asn1SccPID destination, bool is_enabled)
{
    modemanager_ctrl_routing_table[destination] = is_enabled;
}

void modemanager_ctrl_set_recipient(asn1SccPID destination)
{
    modemanager_ctrl_set_all_routes_enabled(false);
    modemanager_ctrl_set_route_enabled(destination, true);
}

void modemanager_ctrl_set_all_routes_enabled(bool are_enabled)
{
    for (size_t i = 0; i < (size_t)PID_env; i++)
    {
        modemanager_ctrl_routing_table[i] = are_enabled;
    }
}
bool modemanager_tm_routing_table[PID_env];


void modemanager_tm_set_route_enabled(asn1SccPID destination, bool is_enabled)
{
    modemanager_tm_routing_table[destination] = is_enabled;
}

void modemanager_tm_set_recipient(asn1SccPID destination)
{
    modemanager_tm_set_all_routes_enabled(false);
    modemanager_tm_set_route_enabled(destination, true);
}

void modemanager_tm_set_all_routes_enabled(bool are_enabled)
{
    for (size_t i = 0; i < (size_t)PID_env; i++)
    {
        modemanager_tm_routing_table[i] = are_enabled;
    }
}



__thread asn1SccPID modemanager_feedback_sender_pid;

asn1SccPID modemanager_feedback_get_sender()
{
    return modemanager_feedback_sender_pid;
}
__thread asn1SccPID modemanager_reading_sender_pid;

asn1SccPID modemanager_reading_get_sender()
{
    return modemanager_reading_sender_pid;
}
__thread asn1SccPID modemanager_tc_sender_pid;

asn1SccPID modemanager_tc_get_sender()
{
    return modemanager_tc_sender_pid;
}




// Routing table of function Sensor

bool sensor_reading_routing_table[PID_env];


void sensor_reading_set_route_enabled(asn1SccPID destination, bool is_enabled)
{
    sensor_reading_routing_table[destination] = is_enabled;
}

void sensor_reading_set_recipient(asn1SccPID destination)
{
    sensor_reading_set_all_routes_enabled(false);
    sensor_reading_set_route_enabled(destination, true);
}

void sensor_reading_set_all_routes_enabled(bool are_enabled)
{
    for (size_t i = 0; i < (size_t)PID_env; i++)
    {
        sensor_reading_routing_table[i] = are_enabled;
    }
}



__thread asn1SccPID sensor_poll_sender_pid;

asn1SccPID sensor_poll_get_sender()
{
    return sensor_poll_sender_pid;
}



