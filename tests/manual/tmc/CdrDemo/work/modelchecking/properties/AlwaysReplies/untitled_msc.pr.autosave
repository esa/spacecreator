system Untitled_MSC;
    /* CIF TEXT (0, 0), (752, 140) */
    use datamodel comment '/home/taste/spacecreator/tests/manual/tmc/CdrDemo/work/simulation/observers/observer.asn';
    signal sig0(SystemCommand) renames input systemTc to ModeManager;
    signal sig1(SystemTelemetry) renames input systemTm to EGSE;
    /* CIF ENDTEXT */
    channel c
        from env to Untitled_MSC with sig0,
sig1;
    endchannel;
    block Untitled_MSC;
        signalroute r
            from env to Untitled_MSC with sig0,
sig1;
        connect c and r;
        /* CIF PROCESS (325, 750), (150, 75) */
        process Untitled_MSC;
            /* CIF TEXT (0, 900), (222, 140) */
            monitor event Observable_Event;
            successstates s2;
            errorstates sErr;
            /* CIF ENDTEXT */
            /* CIF START (4, 1104), (200, 75) */
            START;
                /* CIF NEXTSTATE (69, 1291), (70, 35) */
                NEXTSTATE s0;
            /* CIF state (2765, 1912), (70, 35) */
            state sErr;
            endstate;
            /* CIF state (2465, 1912), (70, 35) */
            state s2;
            endstate;
            /* CIF state (299, 1241), (70, 35) */
            state s1;
                /* CIF input (269, 1296), (130, 35) */
                input sig1(sig1_param0);
                    /* CIF NEXTSTATE (299, 1346), (70, 35) */
                    NEXTSTATE s2;
                /* CIF input (599, 1296), (70, 35) */
                input *;
                    /* CIF NEXTSTATE (599, 1346), (70, 35) */
                    NEXTSTATE sErr;
            endstate;
            /* CIF state (299, 910), (70, 35) */
            state s0;
                /* CIF input (269, 965), (130, 35) */
                input sig0(sig0_param0);
                    /* CIF decision (257, 1015), (154, 50) */
                    decision present(sig0_param0);
                        /* CIF ANSWER (395, 1085), (79, 23) */
                        (doReport):
                            /* CIF NEXTSTATE (399, 1123), (70, 35) */
                            NEXTSTATE s1;
                        /* CIF ANSWER (999, 1085), (70, 23) */
                        ELSE:
                    enddecision;
                    /* CIF NEXTSTATE (299, 1174), (70, 35) */
                    NEXTSTATE s0;
                /* CIF input (599, 965), (70, 35) */
                input *;
                    /* CIF NEXTSTATE (599, 1015), (70, 35) */
                    NEXTSTATE s0;
            endstate;
        endprocess Untitled_MSC;
    endblock;
endsystem;