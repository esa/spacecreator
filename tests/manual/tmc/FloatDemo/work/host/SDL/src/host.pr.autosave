/* CIF PROCESS (250, 150), (150, 75) */
process Host;
    /* CIF TEXT (554, 156), (170, 140) */
    DCL cfg PidConfig;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF task (311, 65), (86, 53) */
        task cfg.kp := 1,
cfg.kd := 2,
cfg.ki;
        /* CIF output (290, 148), (129, 35) */
        output configuration(cfg);
        /* CIF NEXTSTATE (320, 198), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
    endstate;
endprocess Host;