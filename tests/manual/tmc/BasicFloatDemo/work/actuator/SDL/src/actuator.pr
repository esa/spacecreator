/* CIF PROCESS (250, 150), (150, 75) */
process Actuator;
    /* CIF TEXT (603, 10), (170, 140) */
    DCL x T_Float;
    /* CIF ENDTEXT */
    /* CIF START (320, 10), (70, 35) */
    START;
        /* CIF NEXTSTATE (320, 60), (70, 35) */
        NEXTSTATE Wait;
    /* CIF state (450, 10), (70, 35) */
    state Wait;
        /* CIF input (425, 65), (114, 35) */
        input controlsignal(x);
            /* CIF NEXTSTATE (448, 120), (70, 35) */
            NEXTSTATE Wait;
    endstate;
endprocess Actuator;