/* Body file for function SystemControl
 * Generated by TASTE (kazoo/templates/glue/language_wrappers/vm-if-body/function.tmplt)
 */
#include "systemcontrol_vm_if.h"
#include "C_ASN1_Types.h"
#include <stdio.h>
extern long long getTimeInMilliseconds(void);

// Define a global PID variable used to keep track of the sender of the last message
static asn1SccPID sender_pid = PID_env;

// API to provide to the user code the PID of the last PI caller
void systemcontrol_get_sender(asn1SccPID *OUT_sender_pid, asn1SccPID unused)
{
  *OUT_sender_pid = sender_pid;
}



extern void adainit(void);

unsigned systemcontrol_initialized = 0;
void init_systemcontrol(void)
{
   if (0 == systemcontrol_initialized) {
      // Initialize epoch for the MSC tracing in debug mode
      long long unused = getTimeInMilliseconds();
      systemcontrol_initialized = 1;
      // Initialize Ada runtime to call elaboration functions
      adainit();
      puts ("[TASTE] Initialization completed for function SystemControl");
      systemcontrol_initialized = 2;
   }
}
void systemcontrol_reading
      (const char *IN_x, size_t IN_x_len)

{
   sender_pid = systemcontrol_reading_get_sender();
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: systemcontrol,reading,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Declare user code function as external (it may not have a .h if it is in Ada)
   extern void systemcontrol_PI_reading
      (const asn1SccT_Float *);





   // Call user code
   systemcontrol_PI_reading ((asn1SccT_Float *)IN_x);

   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *systemcontrol_state(void);
      printf ("INNER_SDL_STATE: systemcontrol,%s\n", systemcontrol_state());
      fflush(stdout);
   }
}




void systemcontrol_reporting(void)
{
   sender_pid = systemcontrol_reporting_get_sender();
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: systemcontrol,reporting,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Directly call user code, no parameters to pass
   extern void systemcontrol_PI_reporting(void);
   systemcontrol_PI_reporting();
   // puts ("systemcontrol_reporting called");
   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *systemcontrol_state(void);
      printf ("INNER_SDL_STATE: systemcontrol,%s\n", systemcontrol_state());
      fflush(stdout);
   }
}




void systemcontrol_tc
      (const char *IN_x, size_t IN_x_len)

{
   sender_pid = systemcontrol_tc_get_sender();
   // In Debug mode and x86 builts only: log MSC data when environment variable is set
   static int innerMsc = -1;
   if (-1 == innerMsc)
     innerMsc = (NULL != getenv("TASTE_INNER_MSC"))?1:0;
   if (1 == innerMsc) {
      long long msc_time = getTimeInMilliseconds();
      printf ("INNER_PI: systemcontrol,tc,%lld\n", msc_time);
      fflush(stdout);
   }

   //  Declare user code function as external (it may not have a .h if it is in Ada)
   extern void systemcontrol_PI_tc
      (const asn1SccT_Int *);





   // Call user code
   systemcontrol_PI_tc ((asn1SccT_Int *)IN_x);

   // trace the SDL state (appears on the MSC)
   if (1 == innerMsc) {
      extern char *systemcontrol_state(void);
      printf ("INNER_SDL_STATE: systemcontrol,%s\n", systemcontrol_state());
      fflush(stdout);
   }
}

