inline EnvParamSeq_x_generate_value(value)
{
    if
    ::  atomic {
        select (value : 0 .. 9);
    }
    fi;
}
inline EnvParamSeq_y_generate_value(value)
{
    if
    ::  atomic {
        select (value : 0 .. 7);
    }
    fi;
}
inline EnvParamSeq_z_generate_value(value)
{
    if
    ::  true;
        value = 1;
    ::  true;
        value = 0;
    fi;
}
inline EnvParamSeq_generate_value(value)
{
    EnvParamSeq_x_generate_value(value.x);
    if
    ::  true;
        EnvParamSeq_y_generate_value(value.y);
        value.exist.y = 1;
    ::  true;
        value.exist.y = 0;
    fi;
    EnvParamSeq_z_generate_value(value.z);
}
