inline Type1_generate_value(value)
{
    if
    ::  atomic {
        select (value : 0 .. 3);
    }
    fi;
}
inline SimpleVariableSizeSequenceOf_length_generate_value(value)
{
    if
    ::  atomic {
        select (value : 3 .. 5);
    }
    fi;
}
inline SimpleVariableSizeSequenceOf_generate_value(value)
{
    int i;
    SimpleVariableSizeSequenceOf_length_generate_value(value.length);
    for(i : 0 .. value.length - 1)
    {
        Type1_generate_value(value.data[i]);
    }
}
